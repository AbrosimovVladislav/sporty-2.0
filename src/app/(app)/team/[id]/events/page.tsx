"use client";

// TODO (итерация 2): загрузить события команды, отфильтровать по статусу.
// Кнопка "Создать событие" и финансовые индикаторы — только для role === organizer.

export default function EventsPage() {
  return (
    <section className="bg-background-card border border-border rounded-lg p-6 text-center text-foreground-secondary text-sm">
      События появятся здесь
    </section>
  );
}
