# Роадмап MVP 1.5 — Полировка, баги, переработка карточек

> **Контекст.** После роадмапа 1.4 (постраничная переработка дизайна, итерации 30–42.1). В 1.5 закрываем баги и UX-косяки, найденные в живом тестировании, доделываем итерацию 43 из 1.4 и идём по экранам приложения. Итерации профиля игрока v2 + редизайна онбординга вынесены в [роадмап 1.7](roadmap-1.7.md) (прежде были в 1.6 как 53–55, переехали с пересчётом номеров).
>
> **Глобальное правило**, зафиксированное по итогам тестирования: никакие страницы или фиксированные элементы (кроме bottom-sheet'ов) не должны перекрывать нижнее меню. Bottom-sheet — единственное исключение, потому что это временная подстраница.
>
> **Порядок работы.** Каждая итерация делается целиком и проходит ручное тестирование пользователем. Только после прохождения тестов — старт следующей итерации. Зависимости между итерациями явно прописаны в блоках «Не входит / Зависит от».

---

## ✅ Итерация 43 — Аватары игроков + stat-полоса событий (перенос из 1.4)

**Цель.** Легковесные UX-добавки в табах «Состав» и «События» — фотографии в составе и контекст в событиях. Перенесено из роадмапа 1.4 как незакрытое.

### Состав (`/team/[id]/roster`)

- ✅ `PlayerListRow` — аватар прокинут из `team.members[].user.avatar_url`
- ✅ `PlayerSheet` (карточка игрока): `Avatar size="lg"` с реальным `avatar_url`

### Backend

- ✅ `GET /api/teams/[id]` — `users.avatar_url` добавлен к селекту members
- ✅ `TeamMember.user` тип — `avatar_url: string | null`

---

## ✅ Итерация 44 — Концепция города и back-навигация (продуктовое решение)

**Цель.** Закрыть два архитектурных вопроса до старта работы по поиску и страницам 1-го уровня.

### 44.1 Концепция города ✅

**Реализовано.** Город — глобальный scope приложения, хранится в `users.city` (в БД), управляется через `CityContext`. Правила:

- ✅ Главная страница — только отображает город (иконка + название), без возможности менять. Тап на город/имя → переход в профиль
- ✅ Профиль `/profile` — показывает текущий город и район. Тап по кнопке города → `CitySheet` для смены
- ✅ Настройки `/profile/settings` — city chips + `DistrictSelect`, смена сохраняется в `users.city` через `PUT /api/users/[id]/profile`
- ✅ `CityContext.setActiveCity` — при смене города сбрасывает `district_id: null` (избегает «районы от другого города»)
- ✅ Районы привязаны к городу: в БД `districts` с полем `city`; есть данные для всех 10 KZ-городов. `DistrictSelect` фильтрует по текущему городу
- ✅ Поиск использует `activeCity` как scope (без отдельного фильтра «Город» в `FiltersSheet`)

### 44.2 Back-навигация ✅

**Реализовано.** Компонент `BackButton` (`src/components/BackButton.tsx`) — кастомная back-стрелка (не Telegram SDK native, выбрано как более надёжный fallback для браузера):

- ✅ `router.back()` если история есть, иначе `router.push(fallbackHref)`
- ✅ Используется на вторичных экранах (`/profile/settings`, `/players/[id]`, `/teams/create`, `onboarding/layout`, `PageHeader`, `ScreenHeader`)
- ✅ `kind="on-photo"` для hero-экранов с тёмным фоном, `kind="light"` для светлых

**Не входит.** Полный аудит всех роутов на наличие back — делаем точечно по мере прохождения итераций 51–54.

---

## ✅ Итерация 45 — Шапка команды → gear, страница настроек команды

**Цель.** Перевести управление командой на единую точку входа — шестерёнку в шапке `/team/[id]/*`, ведущую на отдельную страницу настроек.

### 45.1 Шапка `/team/[id]/*`: bell → gear ✅

- ✅ Bell удалён, заменён на шестерёнку (`PageHeader.onSettingsClick` + `GearIcon`)
- ✅ Шестерёнка позиционируется `absolute top-5 right-4` (отвязана от flex-строки заголовка → не плавает по вертикали относительно лого/имени)
- ✅ Red-dot при `pendingRequestsCount > 0`
- ✅ Back-стрелка убрана из шапки команды (нативный Telegram back / свайп — основной путь назад)
- ✅ Камера на лого убрана — загрузка лого переехала в настройки

### 45.2 Страница `/team/[id]/settings` (organizer-only) ✅

- ✅ Минимальный header (back + title «Настройки команды»)
- ✅ Загрузка/смена логотипа (через `POST /api/teams/[id]/logo`)
- ✅ Toggle «Ищем игроков» (`looking_for_players`) — переехал с главной команды, instant save
- ✅ Карточка «Заявки и приглашения» → `TeamRequestsSheet` через `useTeamUI().openRequests()` (sheet поднят на уровень layout, доступен и с главной)
- ✅ Inline-edit полей: название, спорт, город, район, описание — общий save через `PATCH /api/teams/[id]`
- ✅ `PATCH /api/teams/[id]` расширен: принимает `name`, `sport`, `city`, `district_id`, `description` (+ старый `looking_for_players`)
- ✅ Player/guest редиректятся на `/team/[id]` (organizer-only access)

**Не входит** (post-1.5): удаление команды, передача организаторских прав.

---

## ✅ Итерация 46 — Единая карточка игрока в команде (`TeamPlayerSheet`)

**Цель.** Унифицировать карточку игрока внутри команды как один паттерн со всех экранов раздела.

### 46.1 Компонент `TeamPlayerSheet` ✅

- ✅ `src/components/team/TeamPlayerSheet.tsx` — bottom-sheet с фиксированной шапкой (avatar 64px, имя, бейдж «Организатор», чипы позиций/уровня/города, кнопка «Открыть профиль» → `/players/[id]`)
- ✅ Аккордеон «Надёжность · в этой команде» — lazy-load из `GET /api/teams/[id]/members/[userId]/reliability` (новый endpoint, team-scoped). Показывает % надёжности, неприходы, отмены, последние 10 событий с цветными точками
- ✅ Аккордеон «Финансы · в этой команде» — lazy-load из существующего `GET /api/teams/[id]/members/[userId]/finances`. Видим только organizer/self. Сальдо (долг/переплата) + breakdown + история платежей
- ✅ Аккордеон «Игровая статистика» — placeholder «🏗 Скоро»
- ✅ Действия: organizer → «Сделать организатором», «Удалить из команды»; self → «Покинуть команду» (с подтверждением)
- ✅ Использует Supabase RLS-эквивалент через requesterId-проверку membership

**Точки входа:**

- ✅ `/team/[id]/roster` — тап на строку (старый inline `PlayerSheet` удалён)
- ✅ `/team/[id]` главная → лидеры — тап открывает sheet (если игрок ещё в составе), иначе fallback на `/players/[id]`
- ⬜ `/team/[id]/finances` → должники — будет в итерации 49

### 46.2 «Лидеры 30 дней» — уточнить смысл ✅

- ✅ Эйбрау переименован на «Лидеры по посещаемости · 30 дней»
- ✅ Тап ведёт на `TeamPlayerSheet` вместо публичного профиля

### 46.3 Создание события: дефолтный город команды + фильтр площадок ✅

**Проблема.** Команда из Астаны видела в форме создания события только площадки Алматы (фетч `/api/venues` без фильтра). Новая площадка тоже хардкодилась с `city="Алматы"`.

**Решение:**

- ✅ Новое поле «Город» в форме создания события — chips со списком `KZ_CITIES`, дефолт = `team.city`
- ✅ Список площадок фетчится с `?city=X` — рефетч при смене города
- ✅ При смене города сбрасываются `selectedVenueId` и `venueDistrictId` (привязаны к старому городу)
- ✅ Empty-state «В городе X пока нет площадок — добавьте новую»
- ✅ Создание новой площадки использует выбранный город (вместо хардкода), `DistrictSelect` тоже city-aware

### Не входит

- Реальная игровая статистика — placeholder-аккордеон (нет трекинга)
- Замена `PlayerSheet` на `TeamPlayerSheet` в `/search/players` — там остаётся переход на публичный профиль (другой контекст)
- Должники в финансах команды → `TeamPlayerSheet` — в итерации 49

---

## ✅ Итерация 47 — Мелкие фиксы команды: позиции, сортировки

**Цель.** Закрыть набор маленьких UX-багов в составе команды и форме настроек профиля. Фиксы независимые, объединены в одну итерацию по размеру.

**Ключевое решение.** Колонка `users.position` переведена с `text` (CSV) на нативный Postgres `text[]`. Существующие данные нормализованы: английские слаги (`midfielder`/`defender`/`forward`/`goalkeeper`) сконвертированы в русские (`Полузащитник`/`Защитник`/`Нападающий`/`Вратарь`) и обёрнуты в массивы. CSV-парсинг убран отовсюду — позиции теперь типизированный массив сквозь весь стек (БД → API → UI).

### 47.1 Состав: убрана опция «Недавние» ✅

- ✅ Опция убрана из `SORT_OPTIONS` в [src/app/(app)/team/[id]/roster/page.tsx](../../src/app/(app)/team/[id]/roster/page.tsx) — оставили «По уровню» и «Сначала организаторы»

### 47.2 Баг профиля: множественные позиции сохраняются криво ✅

**Корень проблемы.** Хранение CSV в колонке `text` + неконсистентный парсинг по приложению (где-то парсилось, где-то нет) → половина просмотровых экранов выводила сырую строку, settings собирал массив через `join(", ")`, форма не пересинхронизировалась с серверным состоянием после `useEffect`.

- ✅ Миграция `ALTER COLUMN position TYPE text[]` (Supabase MCP, имя `user_positions_to_text_array`) с одновременной нормализацией английских слагов в русские
- ✅ `src/types/database.ts` → `position: string[] | null`
- ✅ `PUT /api/users/[id]/profile` принимает массив, валидирует элементы (string + trim + filter empty)
- ✅ `/profile/settings`: убран `split/join`, форма сразу работает с массивом; добавлена синхронизация `setPositions(d.user.position ?? [])` после fetch — больше не висит застывший снимок
- ✅ `/profile`, `/players/[id]`, `TeamPlayerSheet`, `PlayerListRow` — все потребители читают массив напрямую, парсинг CSV удалён

### 47.3 Состав: фильтр по позициям учитывает все амплуа ✅

- ✅ В roster-фильтре `m.user.position === posFilter` заменён на `m.user.position?.includes(posFilter)` — multi-position игрок попадает во все свои чипы
- ✅ В `/api/players` фильтр `eq("position", position)` заменён на `contains("position", [position])` — server-side фильтрация поиска тоже работает корректно (закрывает половину 50.4 заранее)

### 47.4 События команды: исправлена сортировка ✅

- ✅ В [src/app/(app)/team/[id]/events/page.tsx](../../src/app/(app)/team/[id]/events/page.tsx) после деления на `planned/past` добавлена клиентская сортировка: предстоящие — `date ASC` (ближайшее сверху), прошедшие — `date DESC` (последнее завершённое сверху)

### Бонусом

- ✅ Онбординг (`/onboarding/position`) переделан с single-select на multi-select: `OnboardingState.position: string` → `positions: string[]`, action `SET_POSITION` → `TOGGLE_POSITION`. Раньше игроки могли выбрать только одну позицию при первом входе, теперь — несколько. UI остаётся в старой вёрстке (rewrite в дизайн-системе — задача итерации 55.3)
- ✅ `POST /api/onboarding` принимает массив позиций (с обратной совместимостью: одиночная строка тоже принимается и оборачивается в массив)

---

## ✅ Итерация 48 — Форма создания события: layout + auto-fill price

**Цель.** Привести `CreateEventForm` в порядок: стабилизировать вёрстку, автозаполнить цену площадки, починить кнопку.

**Ключевые решения.**

- Поле под «default-цену площадки» в БД названо `default_cost` (не `default_price` как в исходной формулировке) — фактически уже существовало в типах `venues.default_cost`, было задействовано в auto-fill ранее в рамках итерации 46.3 при правках `EventCreateSheet`.
- В sheet'е используется sticky-футер (`borderTop` + `shrink-0`) с `<Button variant="primary" size="lg">` из ДС. Глобальное правило (только bottom-sheet может перекрывать нижнее меню) соблюдается — sticky-кнопка живёт внутри bottom-sheet, не на `BottomActionBar`.
- Native `<select>` оставлен только для площадки (динамический длинный список), украшен SVG-chevron'ом через `background-image`. Все остальные нативные `<select>` (тип, город) переведены на `SheetChipGroup` для визуальной консистентности.

### 48.1 Стабилизация layout формы ✅

- ✅ Все инпуты — фиксированная высота `46px` (`h-[46px]`), `block w-full` + `min-width: 0`
- ✅ Native `<input type="date|time">` заменены на кастомные `DateField` / `TimeField` с bottom-sheet picker'ами. Причина: iOS рисует placeholder с текущей датой/временем серым (нельзя стилизовать), что путает пользователя — поле выглядит как заполненное. Плюс в Telegram WebView нативный picker открывается нестабильно.
  - `DateField`: кнопка с лейблом «Выбери дату» (или `пн, 5 мая` при выбранном значении) → `DatePickerSheet` с 90 днями вперёд, сгруппированными по месяцам, в гриде 3 колонки
  - `TimeField`: кнопка с лейблом «Выбери время» (или `19:30`) → `TimePickerSheet` с грид-сеткой времени 06:00–23:30 в шаге 30 минут (4 колонки)
  - Picker-sheet открывается поверх sheet'а формы (`zIndex: 60`), backdrop кликабелен для закрытия
- ✅ Поле «Город» переведено с нативного `<select>` на `SheetChipGroup` (тот же паттерн, что у «Тип события»)
- ✅ Native `<select>` оставлен только для площадки (длинный динамический список), украшен SVG-chevron'ом через `background-image`
- ✅ `gap-4` между полями (вместо `gap-5`) — плотнее и без прыжков при появлении/скрытии условных полей (`venueMode === "new"`)
- ✅ Numeric-инпуты получили `inputMode="numeric"` для корректной мобильной клавиатуры

### 48.2 Auto-fill цены площадки ✅ (фактически закрыто в 46.3)

- ✅ Колонка `venues.default_cost` (numeric) уже была добавлена и присутствует в типах [src/types/database.ts](../../src/types/database.ts)
- ✅ `GET /api/venues` отдаёт `default_cost` [src/app/api/venues/route.ts](../../src/app/api/venues/route.ts)
- ✅ При выборе существующей площадки в форме — auto-fill `venueCost` из `picked.default_cost`; организатор может перезаписать вручную
- ✅ При создании новой площадки введённая `venue_cost` сохраняется как `default_cost` в `venues` [src/app/api/teams/[id]/events/route.ts](../../src/app/api/teams/[id]/events/route.ts)
- ✅ Подсказки под полем поясняют логику («Подставлена стандартная цена…» / «Эта цена сохранится как стандартная…»)

**NB:** `default_cost` также будет использоваться в итерации 51 на странице площадки `/venues/[id]` для отображения цены за час.

### 48.3 Кнопка «Создать событие» ✅

- ✅ Заменена на `<Button variant="primary" size="lg" className="w-full">` из ДС (`src/components/ui/Button.tsx`)
- ✅ Переехала в sticky-футер sheet'а: отдельный `shrink-0` блок с `borderTop`, не скроллится с формой
- ✅ Использует встроенный спиннер `Button` через `loading={sending}` — больше не «Сохраняю…» текстом

---

## ✅ Итерация 49 — Финансы команды v2.1 (понятный дашборд)

**Цель.** Переработать `/team/[id]/finances` так, чтобы дашборд читался без контекста: понятные лейблы, корректные тренды, осмысленные графики, скрыть длинные простыни в аккордеоны. Убрать фиксированный CTA, перекрывающий нижнее меню.

**Зависит от.** Итерация 46 (`TeamPlayerSheet`) — нужен для тапа на должника (49.4).

**Ключевые решения.**

- Trend «Лучше/хуже прошлого месяца» в hero убран полностью. Сравнение `netDelta` за 30 дней vs предыдущие 30 дней давало шум на маленьких объёмах данных и не учитывало долги/переплаты — больше путало, чем помогало. Тренд переехал в bar-chart как сводка «Чистый: +N ₸».
- Donut «Эффективность сборов» (`collected / expected`) заменён на разделённую полосу «Сборы vs Расходы» с bignum чистой прибыли. Прежняя метрика выходила за 100% при низком `expected` и теряла смысл.
- Депозит — inline-карточка в конце страницы, без `BottomActionBar` и без FAB. Глобальное правило соблюдено: только bottom-sheet перекрывает нижнее меню.
- `TeamPlayerSheet` получил опциональный проп `initialSection: "reliability" | "finances" | "stats"` — позволяет открывать с уже раскрытым нужным аккордеоном.

### 49.1 Hero-KPI и сводка ✅

- ✅ Bignum «Реальный баланс» оставлен (зелёный/красный по знаку), trend-стрелка и подпись «Лучше/хуже прошлого месяца» удалены
- ✅ Ribbon из 3 сегментов переименован: «На счету · касса» / «Должны нам · игроки» / «Должны мы · площадкам». Долговые сегменты подсвечиваются красным, когда сумма > 0
- ✅ Сводка сокращена с 4-5 ячеек до 2: «Собрано за всё время» + «Расходы на площадки». Ожидаемый сбор и переплаты убраны — они уже отражены через долги/переплаты в hero и через bignum маржи

### 49.2 Bar chart «Поток за 6 месяцев» ✅

- ✅ Над каждой колонкой компактное число (`12k`, `1.5M`) на максимуме столбика
- ✅ Легенда «Сборы / Площадки» (вместо `collected` / `venuePaid`)
- ✅ Под графиком справа — bignum-сводка «Чистый: +N ₸» (зелёный) / «Чистый: −N ₸» (красный) — итог `Σ collected − Σ venuePaid` за 6 месяцев

### 49.3 Маржинальность (replaces donut) ✅

- ✅ `CircularProgress` (donut) заменён на горизонтальную полосу с двумя сегментами: «Сборы» (зелёный) + «Расходы» (серый), пропорция = `collected / (collected + venuePaid)`
- ✅ Справа от заголовка — bignum чистой прибыли `±N ₸`, цвет: зелёный если `collected ≥ venuePaid`, красный иначе
- ✅ Под полосой — раскрытые числа: «N ₸ сборы» и «расходы M ₸»
- ✅ Скрывается, когда оба значения = 0 (нет финансовых событий вообще)

### 49.4 Должники / переплатили ✅

- ✅ Прогресс-бар: формула `amount / max(amounts) × 100%`, явно через `Math.max(...debtors.map(...))` (раньше использовался `debtors[0].amount` — корректно благодаря сортировке, но фрагильно). Минимальная видимая длина 8% для строк с малой долей
- ✅ `PlayerCard` (старый sheet) заменён на `TeamPlayerSheet` с `initialSection="finances"` — аккордеон «Финансы · в этой команде» раскрыт сразу. Закрытие открывает заново загрузку финансов страницы (через `onActionDone` и `load()` если действие изменило состав)
- ✅ Если должник/переплативший больше не в составе команды (редкий кейс) — sheet просто не открывается, тап игнорируется

### 49.5 Площадки — аккордеон ✅

- ✅ Простыня «Площадки» свёрнута в аккордеон. Шапка: «Площадки · N событий» (правильное склонение) + строка-сводка «Оплачено N из K · долг M ₸» (долг красный, если > 0)
- ✅ Раскрытие — прежний список событий с `venue_cost > 0`, без визуальных изменений: тип + дата + venue + остаток («оплачено» зелёным или «−M ₸» красным)
- ✅ Шеврон вращается при раскрытии

### 49.6 Депозит — inline-карточка ✅

- ✅ `BottomActionBar` удалён со страницы полностью. `pb-24` (отступ под фикс-бар) тоже убран
- ✅ В конце страницы добавлена inline-карточка `DepositCard`: заголовок «Депозит игрока», подсказка «Пополни баланс игрока, чтобы списывать оплату событий автоматически», primary-кнопка `<Button>` на всю ширину
- ✅ Кнопка открывает прежнюю `DepositModal` (без изменений)
- ✅ Глобальное правило (только bottom-sheet перекрывает нижнее меню) соблюдено: на странице больше нет fixed-CTA

### Не входит

- Тренд «месяц-к-месяцу» в hero — убран сознательно. Если позже понадобится, считать честно через `realBalance` snapshot, а не через `netDelta` транзакций (этого пока нет в data-pipeline)
- Графики на главной странице команды (`/team/[id]`) и на других экранах — не трогали

---

## ✅ Итерация 50 — Поиск: унификация листинг-страниц

**Цель.** Привести 4 таба `/search/*` (события / команды / игроки / площадки) к одинаковому уровню понятности и функциональности.

**Зависит от.** Итерация 44 (концепция города) — определяет, остаётся ли фильтр «Город» в `FiltersSheet`. Итерация 47.3 (фикс multi-position в составе) — на той же логике решается 50.4.

**Ключевые решения.**

- **Stat-карточки** в `PageHeader` обрезаны до 0–2 ячеек: события — «Всего» + «На неделе» (убрано «Сегодня»), команды — «Всего» + «Ищут игроков», игроки — «Всего» + «Ищут команду» (убрано «В моих командах» — на discovery-странице это бесполезно), площадки — `PageHeader` без `HeaderStatGroup` совсем.
- **Sort-наборы:** держим только то, что бэкенд может сортировать через индекс (без `team_memberships(count)` ORDER BY и без position-priority — это потребовало бы denormalized колонок). На команды кладём `created_desc` + `name_asc`, на игроков добавлен `name_asc` + переименовано `recent` → «Недавно зарегистрированные», на события — `date_asc / date_desc / price_asc`. «По размеру» (members_count) и «По позиции» отложены — реализуются через миграцию (denormalize) или RPC.
- **Позиции на `/search/players`** — вариант C: чипы в шапке = единственная точка правды, поле «Позиция» в `PlayerFiltersSheet` удалено. «Универсал» как чип не добавлен (по запросу пользователя — лишний на MVP).
- `**PositionChip**` живёт в `src/components/PositionChip.tsx` (без подпапки `players/`) — используется в `PlayerListRow` и `TeamPlayerSheet`, потенциально расширяется ещё на `52.3 «Обо мне»` и `54.3 публичный профиль`.
- **Дата/время** в форме создания события переделаны на `NativeDateField` / `NativeTimeField` — стайлованная кнопка + скрытый native `<input type="date|time">` + `showPicker()` API. Решение: чистый визуал ДС + системный пикер iOS/Android. Это не custom-календарь (от которого пользователь отказался), а тонкая обёртка вокруг родного.

### 50.1 Убрать дублирование счётчиков «Найдено N» / «Результаты N» ✅

**Проблема.** На всех 4 вкладках сейчас выводятся два счётчика подряд: `ListMeta` сверху («Найдено 4 команды») + эйбрау «РЕЗУЛЬТАТЫ · 4» под ним. Одна и та же информация — лишний шум.

- ✅ Из `ListMeta` убран `countLabel` на всех 4 вкладках. Эйбрау «Результаты · N» оставлен над списком — единственный счётчик. `ListMeta` теперь несёт только sort-dropdown
- ✅ `pluralEvents/Teams/Players/Venues` хелперы удалены вместе с `countLabel`

### 50.2 Stat-карточки в `PageHeader` ✅

- ✅ **События:** оставлены 2 ячейки — «Всего» + «На неделе». Убрана «Сегодня» (почти всегда 0/1, шум на city-scope)
- ✅ **Команды:** оставлены 2 ячейки — «Всего» + «Ищут игроков». «Мои» убраны — counter без контекста бесполезен
- ✅ **Игроки:** оставлены 2 ячейки — «Всего» + «Ищут команду». «В моих командах» убрана — это не для discovery-страницы
- ✅ **Площадки:** `HeaderStatGroup` удалён совсем. `/api/venues/stats` остался, но вызов с фронта убран; PageHeader теперь только title

### 50.3 Сортировки ✅

- ✅ `ListMeta` принимает `sort` без `countLabel` (уже поддерживалось, теперь это единственный режим использования)
- ✅ **События:** dropdown «Сначала ближайшие (default) / Сначала далёкие / По цене (дешевле)». Backend — параметр `sort=date_asc|date_desc|price_asc` в [src/app/api/events/public/route.ts](../../src/app/api/events/public/route.ts)
- ✅ **Команды:** dropdown «Сначала новые (default) / По названию (А-Я)». Backend — параметр `sort=created_desc|name_asc` в [src/app/api/teams/route.ts](../../src/app/api/teams/route.ts). Сортировка «По размеру» **отложена** — `team_memberships(count)` нельзя ORDER BY через Supabase REST без denormalized поля или RPC
- ✅ **Игроки:** dropdown «По уровню (default) / По имени (А-Я) / Недавно зарегистрированные» (`recent` переименован). Backend — параметр `sort=skill|name_asc|recent` в [src/app/api/players/route.ts](../../src/app/api/players/route.ts). «По позиции» **отложена** — требует custom CASE WHEN priority по элементам массива `position`, не покрывается одной order-инструкцией
- ✅ **Площадки:** dropdown скрыт (одна опция бессмысленна). Backend уже сортировал по `name asc` — оставлено как есть

### 50.4 Multi-position фильтр на `/search/players` ✅

- ✅ Backend `/api/players?position=...` уже использовал `contains("position", [position])` (закрыто в 47.3). Ничего менять не пришлось — клиент просто прокидывает позицию из чипов в backend, фильтрация идёт массивно

### 50.5 Дублирование чипов и sheet-фильтра позиций ✅ (вариант C)

- ✅ Поле «Позиция» удалено из `PlayerFiltersSheet` (включая `POSITIONS` импорт и `positionOptions` memo). Тип `PlayerFilters` сжат — поле `position` удалено
- ✅ На странице `/search/players` логика `effectivePosition = positionPill || filters.position` упрощена до `effectivePosition = positionPill`. Active-chip для позиции из шита тоже удалён
- ✅ «Универсал» как отдельный чип **не добавлен** — по запросу пользователя на MVP не нужен. Можно вернуть позже, если найдётся реальный кейс

### 50.6 `PositionChip` ✅

- ✅ Создан [src/components/PositionChip.tsx](../../src/components/PositionChip.tsx) с двумя экспортами: `PositionChip` (одиночный) и `PositionChipList` (массив с gap)
- ✅ Маппинг русских названий → сокращений: «Вратарь» → ВРТ (amber), «Защитник» → ЗАЩ (blue), «Полузащитник» → ПЗЩ (emerald), «Нападающий» → НАП (red)
- ✅ Поддерживает `tone: 'light' | 'dark'`. Light: `bg-{color}-100 text-{color}-700`. Dark (для тёмных hero/PageHeader): `bg-{color}-500/20 text-{color}-200`
- ✅ Применён в `[PlayerListRow](../../src/components/players/PlayerListRow.tsx)` — позиции теперь рендерятся чипами вместо CSV-строки в subtitle. Subtitle сжат до `district || city`
- ✅ Применён в `[TeamPlayerSheet](../../src/components/team/TeamPlayerSheet.tsx)` — старые pill'ы с `PositionIcon` заменены на `PositionChipList`. Локальная иконка `PositionIcon` удалена
- ✅ «Универсал» НЕ маппится — `POSITION_MAP` вернёт `undefined` и чип просто не отрендерится

---

## ✅ Итерация 51 — Страница площадки `/venues/[id]` + фильтры событий

**Цель.** Закрыть оставшийся неработающий переход в каталоге площадок (тап на площадку → ничего) — создать страницу профиля площадки. Параллельно расширить фильтры событий поиска (date range + цена + свободные места).

### 51.1 Страница площадки `/venues/[id]` ✅

Новый роут `/venues/[id]`. Тап по строке в `/search/venues` ведёт сюда.

**Структура.** Dark-hero (фото 230px из `venues.photo_url`, градиент в `gray-900`, back-tablet top-left, название Oswald uppercase 24px, адрес 13px, чипы «Район» и «Цена/час» из `default_cost`) → primary-кнопка «Маршрут» (открывает 2GIS) → секция «Контакты» (телефон, сайт; каждый — ссылка-строка с зелёной иконкой) → секция «Описание» (`bg-secondary` блок) → секция «Ближайшие события · N» с `EventListRow` (первые 5) и кнопкой «Все события на площадке» → `/search/events?venue=<id>`. Спорт-чип не показываем — пока в проекте 1 спорт.

**Backend.** Миграция `20260430000001_venues_contacts.sql` добавила `venues.phone`, `venues.website`, `venues.description`. Эндпоинт `GET /api/venues/[id]` возвращает площадку + первые 5 ближайших публичных событий + общее их число. `GET /api/venues` теперь отдаёт `photo_url`.

### 51.2 Поиск/события: фильтр по дате ✅

В `EventFiltersSheet` добавлено поле «Период»: пресеты «Любой» / «Сегодня» / «На этой неделе» / «На следующей» / «На 2 недели» + два нативных `<input type="date">` (от–до). Пресет и custom-range взаимоисключающие — выбор пресета чистит поля и наоборот. Активный фильтр выводится чипом — либо лейбл пресета, либо `DD.MM — DD.MM`.

**Backend.** `GET /api/events/public` принимает `from` / `to` (YYYY-MM-DD). При `from` фильтр `> now()` заменяется на `>= from`.

### 51.3 Поиск/события: дополнительные фильтры ✅

В `EventFiltersSheet` добавлены:

- **Цена** — `SheetChipGroup` с пресетами: «Бесплатно» / «До 1 000 ₸» / «До 2 000 ₸» / «До 3 000 ₸». Параметр API: `price_max=<int>`.
- **Только со свободными местами** — toggle. Параметр API: `has_spots=true`. Считается на стороне сервера через post-fetch фильтр (`yes_count < min_players`); при включении эндпоинт over-fetch'ит до 200 строк и режет в памяти.

**Не вошло (отложено).**

- **Тип события** — остаётся inline-pills под поиском (не дублируем в sheet).
- **Только моих команд** — зависит от ит. 31 (фильтр `?my=1`), не реализованной.
- **Спорт** — пока в проекте 1 спорт.

### Переход с страницы события

`EventVenueCard` опциональный тап «Подробнее о площадке» → отложили (post-MVP).

### Не вошло (post-MVP)

- Загрузка фото площадки пользователем
- Отзывы / рейтинг площадок

---

## ✅ Итерация 52 — Профиль игрока v3: редизайн + настройки

**Цель.** Привести `/profile` и `/profile/settings` в визуальное соответствие с остальным приложением. Сейчас профиль — единственная страница 1-го уровня без зелёного `PageHeader`, сделан в старых компонентах. Полный визуальный refresh в дизайн-системе + точечные UX-фиксы внутри табов.

**Зависит от.** Итерация 50.6 (`PositionChip`) — для отображения позиций в табе «Обо мне».

### 52.1 Шапка профиля: зелёный `PageHeader` с фото + gear-иконка ✅

**Проблема.** Сейчас `/profile` использует светлый custom-hero с круглым avatar в центре. Это единственная страница 1-го уровня без зелёного `PageHeader`. Визуально профиль «выпадает» из приложения.

**Решение.**

- Перейти на `PageHeader` (зелёный с диагональной текстурой, скругление 28px снизу) — как `/teams`, `/search/`_, `/team/[id]/_`
- Аватар пользователя — по аналогии с лого команды (см. ит. 41), круглый avatar 56-80px **слева** перед title-блоком (имя + город · район subtitle). Если фото не загружено — градиент с инициалом
- Иконка камеры-бейдж 20px поверх аватара → загрузка фото (механика та же, что у `team-logo` в ит. 42.1)
- Stat-карточки в шапке (`HeaderStatGroup`): 2-3 ключевые метрики на тёмной плашке. Кандидаты: «Сыграно · N», «Надёжность · X%», «Команд · K». Финальный набор — при имплементации
- **Шестерёнка top-right** (`onSettingsClick`) → `/profile/settings`. Стиль — в одном ряду с gear-кнопкой из шапки команды (45.1), чтобы паттерн узнавался во всех шапках 1-го уровня
- Дополнительный бейдж «Ищет команду» (если включено) — рядом с subtitle или как отдельная плашка

### 52.2 Табы профиля: фикс вёрстки + единый стиль с `UnderlineTabs` ✅

**Проблема.** Сейчас 4 таба (Обо мне / Результаты / Надёжность / Достижения) реализованы инлайн state-based, и «Достижения» уезжает в упор к правому краю экрана — не помещается. Стиль не идентичен `UnderlineTabs` из других страниц.

**Решение.**

- Перевести табы профиля на общий примитив `UnderlineTabs` (тот же, что в `SearchSubnav` / `TeamSubNav`)
- Каждый таб `flex-1` — равная ширина, чтобы 4 таба нормально умещались на узких экранах
- Если 4 таба всё равно не влезают — уменьшить горизонтальный padding или сократить названия («Достижения» → «Награды»)

### 52.3 «Обо мне»: redesign в компонентах дизайн-системы

**Проблема.** Сейчас контент таба — старые `Card`/`MiniStatCard`/`SectionEyebrow` обёртки или инлайн-вёрстка с прежними цветами. Раздел не редизайнили со старого MVP. Контент сам по себе ок (био, 2×2 stat «Уровень / Возраст», позиции, время тренировок), но визуально выглядит дёшево.

**Решение.**

- Полный rewrite таба «Обо мне» в дизайн-системе: токены `--bg-primary`, `--text-primary`, `--green-600` и т.д.
- Био — карточка `bg-bg-secondary` с типографикой ДС
- 2×2 stat (уровень + возраст) — `StatTile` или эквивалент, как на главной
- Позиции (амплуа) — `PositionChip` (см. 50.6) — цветные чипы с сокращениями
- Empty-state с CTA «Заполнить профиль» → `/profile/settings`
- Время тренировок — **убираем из отображения** (см. 52.6, поле скрывается из настроек тоже)

### 52.4 «Результаты»: явный stage-of-development placeholder ✅

**Проблема.** Сейчас в табе «Результаты» — bignum «Сыграно матчей» + 2×2 placeholder-карточки (Голы / Передачи / Жёлтые / MVP). Под placeholder'ами лейблы «скоро» — но это выглядит как недоделанный экран.

**Решение.**

- Сохранить bignum «Сыграно матчей» (Oswald 40px) — это реальная метрика
- Placeholder для остальных метрик — заменить на одно крупное info-сообщение «🏗 Игровая статистика — в разработке. Скоро ты сможешь видеть свои голы, передачи, MVP-награды и другие показатели»
- Убрать 4 placeholder-карточки (Голы/Передачи/Жёлтые/MVP) — их визуальная пустота создаёт впечатление сломанного экрана

### 52.5 «Надёжность»: фикс списка событий ✅

**Проблема.** Сейчас в табе:

- Список последних событий показывает «-22 дня» (относительные дни от текущей даты в минус). Непонятно
- Сортировка событий — нужно проверить, но скорее всего от старых к новым; пользователь хочет «недавно прошедшие сверху»
- Формат строки — нужна дата + название команды + статус (был/не был/не проголосовал)

**Решение.**

- Сортировка: `start_at DESC` (самое свежее завершённое событие — сверху)
- Формат строки: «3 мая · Команда «Мельник» · Был» (с цветной точкой статуса слева)
- Убрать «-22 дня» полностью — заменить на абсолютную дату («3 мая» / «Пн, 12 апр»)
- Цветные точки статуса оставить как сейчас

### 52.6 Настройки `/profile/settings` v2: redesign в ДС + UX-фиксы ✅

**Проблема.**

- `/profile/settings` визуально не вписан в дизайн-систему (старая вёрстка, формы как до ит. 38)
- Поле «Район» — не выглядит как dropdown, при тапе вылезает что-то непонятное
- Поле «Время тренировок» — пресет-чипы, не используется содержательно. Проще убрать

**Решение.**

- Полный rewrite `/profile/settings/page.tsx` в компонентах ДС: `Input`, `Textarea`, `SheetChipGroup`, `Picker`, `Button`, `BottomActionBar`
- Шапка — back-arrow + title «Настройки профиля» (минимальный header без зелёного PageHeader, как все вторичные экраны — паттерн идентичен `/team/[id]/settings` из 45.2)
- Поле «Район» — заменить на полноценный `Picker` или sheet-селектор (как `DistrictSelect`, но визуально явный dropdown с иконкой `ChevronDown`)
- Поле «Время тренировок» — **убрать совсем** (и из формы, и из таба «Обо мне» — см. 52.3)
- Sticky `BottomActionBar` снизу с primary-кнопкой «Сохранить» — без перекрытия нижнего меню (см. глобальное правило)
- Поля: имя (если редактируется), bio, амплуа (multi-select chips), уровень (chip group), город (Picker), район (Picker), дата рождения (native date), «Ищу команду» (toggle-row)

---

## 🔄 Итерация 1.5.1 — Code review, рефакторинг, оптимизация

**Цель.** Убрать лаги и фризы при открытии экранов, причесать код. После аудита приоритеты ясны: основной тормоз — init-waterfall (Telegram → Auth → Team → таб), refetch при смене табов, неэффективные API-роуты с 5–9 последовательными запросами, и компоненты-гиганты на 700–1000+ строк.

> **Порядок.** Сначала производительность (1.5.1.1–1.5.1.4) — это даёт мгновенный эффект для пользователя. Затем уборка кода (1.5.1.5–1.5.1.10). Декомпозицию компонентов-гигантов (1.5.1.7) можно выполнять параллельно после того, как новые границы зафиксированы.

> **Статус по итогам тестирования первой волны.** Производительность сети и render-tree улучшилась (см. ✅ ниже), но при тестировании выявлен второй слой проблем: визуальный jank при переключении табов, layout shift на главной команды и в карточке игрока — данные дозаполняются и компоненты «прыгают». Добавлены пункты 1.5.1.13–1.5.1.15 — точечные UX-фиксы без крупных архитектурных изменений (роутинг сохраняется).

### 1.5.1.1 Init-waterfall: убрать блокировку первого рендера ✅

**Проблема.** Сейчас порядок инициализации последовательный:

1. `TelegramProvider` ([src/components/TelegramProvider.tsx](../../src/components/TelegramProvider.tsx)) возвращает `null` пока `setReady(true)` не отработает после загрузки `telegram-web-app.js` → белый экран
2. `AuthProvider` дожидается ответа `/api/auth/telegram` (мс)
3. `TeamProvider` ждёт `auth.status !== "loading"`, потом fetch'ит `/api/teams/[id]`
4. Страница таба ждёт `team.status === "ready"`, потом fetch'ит свои данные

Итого 3–4 последовательных round-trip до первого осмысленного пикселя.

**Решение.**

- ✅ `TelegramProvider`: убран `if (!ready) return null`, рендер children сразу, `twa.ready()/expand()` живёт в `useEffect`
- ✅ `AuthProvider`: добавлен sessionStorage-кэш `user` (`SESSION_USER_KEY`), value обёрнут в `useMemo`, lazy init из кэша → мгновенный возврат после reload
- ✅ `TeamProvider`: in-memory `Map<teamId::userId, ReadyTeamData>` ([team-context.tsx](../../src/app/(app)/team/[id]/team-context.tsx)). Initial state читает кэш синхронно, на 404 кэш сбрасывается, при transient ошибках сохраняется last-good

### 1.5.1.2 Контексты: убрать лишние ре-fetch'и и перерисовки ✅

- ✅ `**TeamSubNav` — `onClick: reload` удалён, переход по табам больше не дёргает `/api/teams/[id]`
- ✅ `**CityProvider`** — value обёрнут в `useMemo`, отслеживается `userId/userCity` напрямую
- ✅ `**AuthProvider**` — `useMemo` для value
- ✅ `**TeamProvider**` — `useMemo` для value
- ✅ **TeamPageHeader** — fetch `/api/users/[id]/teams` остался, но переключение табов больше не вызывает новых фетчей

### 1.5.1.3 API: распараллелить запросы внутри роутов ✅

**Сделано.**

- ✅ `GET /api/teams/[id]` — переписан в 2 фазы через `Promise.all` (8 параллельных запросов в фазе 1, attendances только если organizer)
- ✅ `GET /api/teams/[id]/events` — membership проверка распараллелена с full events fetch, post-filter по `is_public` для гостя в JS
- ✅ `GET /api/teams/[id]/finances` — фаза 1: `Promise.all([membership, events, transactions])`; фаза 2: attendances если есть completed events
- ✅ `GET /api/teams/[id]/insights` — полный rewrite. Один events fetch с `eventsLowerBound = min(prevWindowStart, sixMonthsAgo)`, batch-attendances для current/prev/nextEvent. Сократили с 5–9 sequential запросов до 4 параллельных + 1 follow-up

### 1.5.1.4 Дублирующие fetch'и при смене табов ✅

- ✅ **Финансы** — убран отдельный fetch `/api/teams/[id]` для модала депозита, members берётся из `team-context.members` через `useMemo`
- ✅ **Финансы** — должники/переплатили считают `Math.max` один раз в верху компонента, не в каждой строке

### 1.5.1.5 Мемоизация списков ✅

- ✅ `PlayerListRow`, `EventListRow`, `TeamListRow`, `VenueListRow` — обёрнуты в `React.memo` (паттерн `XImpl` + `export const X = memo(XImpl)`)
- ✅ Страницы со списками используют `useMemo` для filtered/sorted массивов

### 1.5.1.6 Изображения ✅

- ✅ Лого команды переведено на `next/image` в 3 местах: [layout.tsx](../../src/app/(app)/team/[id]/layout.tsx), [settings/page.tsx](../../src/app/(app)/team/[id]/settings/page.tsx), [TeamRequestsSheet.tsx](../../src/components/team/TeamRequestsSheet.tsx) — с правильными `sizes` и `priority` для hero
- ✅ `next.config.ts` — `remotePatterns` расширены для Supabase Storage, `images.unsplash.com`, `t.me`

### 1.5.1.7 Декомпозиция компонентов-гигантов ✅

Файлы >500 строк трудно поддерживать и провоцируют каскады ре-рендеров. Разбить на под-компоненты:


| Файл                          | Строк      | Разбить на                                                                                                                                                | Статус |
| ----------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| `profile/page.tsx`            | 1082 → 273 | `_components/{AboutTab, ResultsTab, ReliabilityTab, AchievementsTab, MyJoinRequests, atoms, types}`                                                       | ✅      |
| `TeamPlayerSheet.tsx`         | 929 → 322  | `team-player-sheet/{types, icons, atoms, Accordion, ReliabilityBody, FinancesBody, SheetHeader}`                                                          | ✅      |
| `team/[id]/page.tsx`          | 876 → 109  | `_components/{NextEventCard, ActivityCard, TopPlayersCard, FinanceCard, RequestsCounter, GuestJoinBar, EmptyTeamHome, SkeletonHome, atoms, icons, types}` | ✅      |
| `team/[id]/finances/page.tsx` | 749 → 157  | `_components/{FinancesHero, MetricsBreakdown, FlowChart, MarginBar, DebtorsList, VenuesAccordion, DepositCard, DepositModal, types}`                      | ✅      |
| `team/[id]/events/page.tsx`   | 735 → 122  | `_components/{EventsListSkeleton, EventsSection, EventCreateSheet, NativeDateTimeFields, icons, constants, types}`                                        | ✅      |


**Подход.** Файлы режем по очереди (не пакетом), без изменения поведения — только move/extract. После каждого — `npm run build` для контроля. `_components/` (с underscore) — Next.js не делает роутом, это идиоматичное место для приватных компонентов страницы.

**Профиль (✅).** В `page.tsx` остался shell: `ProfilePage` (auth gate) + `ProfileContent` (header c аватаром, tabs state, fetch'и `/users/[id]`, `/users/[id]/stats`, `/users/[id]/teams`). 4 таба + `MyJoinRequests` + атомы (`Eyebrow`, `StatTile`, `SkeletonBlock`) вынесены в `_components/`. Поведение не менялось.

**TeamPlayerSheet (✅).** В `TeamPlayerSheet.tsx` остался shell: state (openSection, busy, reliability/finances), 2 useEffect-фетча (reliability/finances), 3 handler'а (promote/remove/leave), layout с тремя Accordion'ами и блоками действий. Вынесено в `team-player-sheet/`: типы (`ReliabilityData`, `FinancesData`, `PeekContent`, `TeamPlayerSheetMember`), 5 иконок, атомы (`SkillBadge`, `MiniStat`, `Empty`, `SkeletonRow` + `skillToNum`), `Accordion`, `SheetHeader`, `ReliabilityBody` (+ `peekReliability`), `FinancesBody` (+ `peekFinances`). `lazy.ts` не менялся — путь к компоненту прежний.

**Главная команды (✅).** В `team/[id]/page.tsx` остался shell: `TeamHomePage` (insights state + fetch, activePlayer state, dispatch на skeleton/empty/main, координация `TeamPlayerSheet` через `handleLeaderClick`). 8 секционных карточек вынесены в `_components/`: `NextEventCard` (hero с фоном венью), `ActivityCard` (счётчик событий + bars + явка), `TopPlayersCard` (3 лидера), `FinanceCard` (organizer-only Δ-блок), `RequestsCounter` (зелёный pill с pending count), `GuestJoinBar` (BottomActionBar для гостя со всеми режимами заявки), `EmptyTeamHome`, `SkeletonHome`. Атомы (`Eyebrow`, `TrendChip` + `initial`/`firstName`/`requestsLabel`), 4 иконки, тип `Insights` — отдельные файлы.

**Финансы команды (✅).** В `finances/page.tsx` остался shell: `TeamFinancesPage` (state data/insights/openMember/showDeposit/error, `load()` с двумя параллельными fetch'ами, useEffect, координация `TeamPlayerSheet`/`DepositModal`, `openPlayer` handler). 8 секционных компонентов вынесены в `_components/`: `FinancesHero` (чёрный hero с realBalance + 3 сегмента в кассе/долгах, локальный `HeroSegment`), `MetricsBreakdown` (сводка с локальным `MetricCell`), `FlowChart` (SVG bar chart за 6 месяцев + локальный `compactNum`), `MarginBar`, `DebtorsList` (объединил блоки «Должны»/«Переплатили» через проп `variant: "debtor" | "creditor"`, локальный `DebtorRow`), `VenuesAccordion` (с локальным `ChevronDownIcon`), `DepositCard`, `DepositModal`. Типы `FinancesData`/`FlowMonth`/`InsightsFinance`/`Member` — в `types.ts`.

**События команды (✅).** В `events/page.tsx` остался shell: `EventsPage` (state events/typeFilter/showCreate, `loadEvents`, useEffect, фильтрация и сортировка `planned`/`past`, FAB и интеграция `EventCreateSheet`). Вынесено в `_components/`: `EventsListSkeleton` (фильтр + 4 строки), `EventsSection` (унифицировал две секции через проп `muted` для прошедших, инлайн `Eyebrow` склеен с заголовком), `EventCreateSheet` (большой sheet со всеми полями: тип, дата/время, цена, мин-игроки, город, площадка, описание, public toggle), `NativeDateTimeFields` (`NativeDateField`/`NativeTimeField` + локальный `formatDateLabel` с RU-таблицами недели и месяцев), 4 иконки (`PlusIcon`/`CloseIcon`/`CalendarIcon`/`ClockIcon`). Типы и константы — отдельные файлы.

### 1.5.1.8 Дедупликация утилит ✅

- ✅ Общий `pluralize(n, [one, few, many])` в [src/lib/format.ts](../../src/lib/format.ts), 5 локальных копий заменены
- ✅ `formatMonthShort` перенесён из `finances/page.tsx` в `format.ts`
- ⏸ Прямые `toLocaleDateString` (~23 места) — оставили, расширим хелперы при следующих касаниях

### 1.5.1.9 Мёртвый код, типы, console.log ✅

- ✅ Удалён `src/components/PlayerCard.tsx` (147 строк, 0 импортов)
- ✅ Прогрепаны и удалены все `as any` / `: any`. Жертвы — два notify-helper'а в API: `notifyMembers` ([events/route.ts](../../src/app/api/teams/[id]/events/route.ts)) и `notifyPlayer` ([invites/route.ts](../../src/app/api/teams/[id]/invites/route.ts)). Заменили `supabase: any` на `ReturnType<typeof getServiceClient>`, `event: any` — на узкий `NotifyEvent { id, type, date, venue_id }`. После этого Supabase-types вскрыли отсутствующую relation между `team_memberships` и `users` — переписали блок выборки telegram_id на двухэтапный pipeline (`map → filter` с type predicate) и cast `as unknown as MemberWithTelegram[]` (тот же паттерн, что для `EventWithVenue` в этом же файле). Ноль `: any` в `src/`
- ✅ Клиентские `console.log/error` — нет в коде. Все 41 вхождение `console.error` живут в API роутах (`src/app/api/`* + `src/lib/telegram-bot.ts`) — это server-side логирование для Vercel logs, оставляем как есть

### 1.5.1.10 Race conditions и cleanup ⏸

Паттерн `let cancelled = true` внутри useEffect консистентен и работает корректно. Унификация в `useFetchData(url, deps)` с `AbortController` отложена — небольшой выигрыш в строках кода, риск регрессий выше пользы. Делаем при следующем крупном рефакторе.

### 1.5.1.11 Dynamic imports для тяжёлых sheet'ов ✅

- ✅ Создан [src/components/team/lazy.ts](../../src/components/team/lazy.ts) — `TeamPlayerSheet`, `TeamRequestsSheet` через `dynamic(() => import(...), { ssr: false })`
- ✅ 5 импорт-сайтов (`layout.tsx`, `roster/page.tsx`, `finances/page.tsx`, `team/[id]/page.tsx`) подключены к `lazy.ts`
- ⏸ `EventCreateSheet`, `DepositModal`, остальные `FiltersSheet` — будут переведены при касании их экранов (низкая стоимость, делаем точечно)

### 1.5.1.13 Optimistic tab activation ✅

**Проблема (по итогам тестирования).** При тапе на табу внутри `/team/[id]/` (Главная / Состав / События / Финансы) видно паузу 100–300ms между касанием и сменой подсветки на новой табе. Технически: `pathname` обновляется только после смены роута → `usePathname()` отдаёт старое значение → indicator застрял на старой табе. Сами данные на новой табе грузятся ок (≈1 сек), но именно «зависание» подсветки бесит.

**Решение.**

- В [src/components/ui/UnderlineTabs.tsx](../../src/components/ui/UnderlineTabs.tsx) добавить локальный state `pendingHref`. На `onPointerDown` (быстрее `onClick`) сохраняем href тапнутой табы → indicator подсвечивает её мгновенно.
- Когда внешний `tabs[i].active` догоняет `pendingHref`, очищаем state.
- Роутинг сохраняется (Link → `router.push` под капотом).

### 1.5.1.14 Стабильные skeleton'ы (no CLS) ✅

**Проблема.** На главной команды (`/team/[id]`) скелетоны NextEvent/Activity/TopPlayers/Finance имеют примерные высоты, не совпадающие с реальной геометрией карточек на 10–30px. При замене скелетона на контент происходит вертикальный «прыжок» — глаз воспринимает как лаг.

**Решение.**

- Замерить реальную высоту каждой карточки (с типичным контентом) и подогнать `h-[Npx]` скелетона. NextEvent ≈ 220px (фото 120 + контент 100), Activity ≈ 156px, TopPlayers ≈ 148px, Finance ≈ 96px.
- Скелетоны на `/team/[id]/roster` и `/team/[id]/events` — использовать ту же высоту строки `64px` (что у `PlayerListRow`/`EventListRow`).

### 1.5.1.15 TeamPlayerSheet: peek без layout shift ✅

**Проблема.** При тапе на игрока sheet открывается мгновенно с шапкой, ниже три аккордеона: «Надёжность», «Финансы», «Игровая статистика». В свёрнутом состоянии каждый аккордеон показывает peek-info справа от заголовка (например `85% · 1 неприход`). Сейчас peek рендерится, только когда `reliability/finances !== undefined` → пока данные грузятся, аккордеон выглядит «пустым» и без secondary-строки → когда данные приходят, аккордеон становится выше → визуальный «прыжок».

**Решение.**

- В `peekReliability`/`peekFinances` для `undefined` (loading) возвращать placeholder с теми же измерениями: `primary: "—"`, `secondary: "Загрузка"`, серый цвет.
- Это сохраняет высоту row'а ровно такой же, как при загруженных данных. Когда данные приходят — текст меняется, высота не меняется.

### Не входит

- Пагинация, server-side агрегаты, индексы БД, React Query, виртуализация — вынесены в итерацию 1.5.3 (готовность к нагрузке)
- Telegram CDN / hosting — деплоится через Vercel, на 1.5 не трогаем

---

## ✅ Итерация 1.5.3 — Готовность к нагрузке (пагинация, индексы, агрегаты)

**Цель.** Подготовить приложение к нагрузке первых 3–6 месяцев: 1000–2000 игроков, 1000–5000 событий, десятки команд. Без индексов и пагинации на этой нагрузке `/search/players` отдаёт ~1MB JSON, события команды грузят сотни attendance-строк, sequential scan упирается в десятки мс. Решаем тремя пластами: индексы → серверные агрегаты → cursor-пагинация через TanStack Query.

**Зависит от.** Итерация 1.5.2 (seed-данные) — желательно, но не блокирующее: без seed индексы и пагинация делаются «вслепую», без EXPLAIN ANALYZE на реалистичном объёме. При наличии живых данных можно начинать с индексов.

**Ключевые решения.**

- **Индексы — первое и обязательное.** Без них `ORDER BY date DESC LIMIT 20` всё равно сканирует таблицу. Пагинация без индексов не спасает, индексы без пагинации тоже
- **Cursor-пагинация, не offset.** На быстрорастущих списках offset плывёт (вставка между запросами рушит порядок) и деградирует на больших страницах. Cursor стабилен, индексируется, корректно работает с infinite scroll. Курсор = `(date, id)` для событий, `(created_at, id)` для команд/игроков
- **Server-side агрегаты.** `yesCount`/`noCount`/`expectedCollected`/`actualCollected` считаем агрегатным SQL в роуте (`COUNT FILTER`, `SUM FILTER`, `LEFT JOIN LATERAL`). RPC-функция — только если запрос дублируется в 2-3 местах
- **Lazy event details.** Полные attendances/transactions нужны только при открытии конкретного события. В listing отдаём только summary
- **TanStack Query для пагинации.** Точечно на 4 пагинированных экранах — `useInfiniteQuery` решает накопление страниц, dedup, инвалидацию. Остальные ~25 fetch-сайтов на `useEffect` остаются как есть

**Порядок работы.**

1. **1.5.3.6** — индексы первым, до всего остального
2. **1.5.3.4 + 1.5.3.5** — серверные агрегаты в роутах + убрать сырые массивы из listing
3. **1.5.3.7 + 1.5.3.1 / 1.5.3.2 / 1.5.3.3** — поставить TanStack Query, переписать 4 экрана на cursor + `useInfiniteQuery`
4. **1.5.3.8** — виртуализация откладывается, возвращаемся при подтверждённой проблеме рендера

### 1.5.3.6 Индексы БД ✅

Миграция `perf_indexes_1_5_3_6` (через Supabase MCP) добавила 11 индексов под точечные горячие запросы. Каждый индекс привязан к конкретному запросу из роута:

- ✅ `idx_events_team_date` — `(team_id, date DESC)` — пагинация событий команды (`/api/teams/[id]/events`)
- ✅ `idx_events_public_date` — partial `(date) WHERE is_public AND status='planned'` — публичный поиск (`/api/events/public`)
- ✅ `idx_events_venue_date` — partial `(venue_id, date DESC) WHERE venue_id IS NOT NULL` — события на странице площадки
- ✅ `idx_event_attendances_user` — `(user_id)` — батч надёжности игроков, история (существующий unique `(event_id, user_id)` не покрывает leading user_id)
- ✅ `idx_financial_transactions_event_type` — `(event_id, type)` — агрегация event_payment'ов
- ✅ `idx_team_memberships_team` — `(team_id)` — состав команды (существующий unique `(user_id, team_id)` не покрывает leading team_id)
- ✅ `idx_teams_city_created` — `(city, created_at DESC)` — discovery с city scope
- ✅ `idx_teams_created` — `(created_at DESC)` — fallback сортировка
- ✅ `idx_users_city_skill` — partial `(city, skill_rank DESC NULLS LAST) WHERE onboarding_completed` — игроки по уровню в городе
- ✅ `idx_users_city_created` — partial `(city, created_at DESC) WHERE onboarding_completed` — recent-сортировка игроков
- ✅ `idx_users_looking` — partial `WHERE looking_for_team` — фильтр «ищет команду»

**Не входит.** EXPLAIN ANALYZE-baseline в `docs/tech/perf-baseline.md` отложен до 1.5.2 (без seed нет реалистичного объёма для замеров).

### 1.5.3.7 TanStack Query ✅

- ✅ Установлен `@tanstack/react-query`
- ✅ `src/lib/query-client.tsx` — `QueryProvider` с дефолтами: `staleTime: 30s`, `gcTime: 5m`, `refetchOnWindowFocus: false`, `retry: 1`
- ✅ `QueryProvider` обёрнут в корневом `src/app/layout.tsx` внутри `AuthProvider`
- ✅ Используется только на 4 пагинированных экранах через `useInfiniteQuery` (см. 1.5.3.1–1.5.3.3). Остальные ~25 fetch-сайтов остаются на `useEffect`

### 1.5.3.4 + 1.5.3.5 Server-side агрегаты + lazy details ✅

- ✅ `/api/teams/[id]/events` отдаёт только агрегаты (`yesCount`, `noCount`, `myVote`, `expectedCollected = attended * price`, `actualCollected = sum(transactions)`); сырые массивы attendances/transactions клиенту не уходят
- ✅ Агрегаты считаются на сервере в JS по компактному батчу: после cursor-пагинации `eventIds.length ≤ 20`, поэтому `IN (...)` к `event_attendances` и `financial_transactions` возвращает максимум ~200 строк. RPC `get_team_events_with_stats` не вынесен — overhead не оправдан, оставлено как опция, если этот же агрегат понадобится в 2-3 местах
- ✅ Полные attendances/transactions грузятся только при открытии конкретного события через `/api/teams/[id]/events/[eventId]` (отдельный endpoint)

### 1.5.3.1 Cursor-пагинация: события команды ✅

- ✅ Хелпер `src/lib/cursor.ts`: `encodeCursor`, `decodeCursor`, `keysetClause` (PostgREST-формат `or=(field.gt."v",and(field.eq."v",id.gt.id))` для tie-break по id)
- ✅ `GET /api/teams/[id]/events` параметры: `direction=upcoming|past`, `cursor`, `limit≤50` (default 20). Default `upcoming` — `date >= now()` ASC, past — `date < now()` DESC. Ответ: `{ events, nextCursor }`. Over-fetch на 1 строку для определения `hasMore` без лишнего round-trip
- ✅ `/team/[id]/events` — два `useInfiniteQuery` (upcoming + past) с одним `queryKey` префиксом `["team-events", teamId, userId, ...]`, два `InfiniteScrollSentinel`. Создание события → `queryClient.invalidateQueries({ queryKey: ["team-events", teamId] })`

### 1.5.3.2 Cursor-пагинация: публичные события ✅

- ✅ `GET /api/events/public` переписан: убран `count: exact` (медленный sequential count со всеми фильтрами), убран `nextOffset`, добавлен `cursor` + `nextCursor`
- ✅ Cursor зависит от сортировки: `date_asc/date_desc` → `(date, id)`, `price_asc` → `(price_per_player, id)` (риск пропуска при коллизии цены принят, tie-break по id страхует)
- ✅ `has_spots` оставлен с over-fetch (`dbLimit = limit * 4`) + JS-фильтрацией по `yes_count < min_players` — отмечено как MVP-компромисс. Денормализованный `events.yes_count` — отдельная пост-задача
- ✅ `/search/events` — `useInfiniteQuery` с ключом `["public-events", queryString]`. Метка «Результаты · N+» — счётчик загруженных, не total

### 1.5.3.3 Cursor-пагинация: команды и игроки ✅

- ✅ `GET /api/teams` — cursor `(created_at, id)` для `created_desc`, `(name, id)` для `name_asc`. Убран `count: exact`
- ✅ `GET /api/players` — cursor `(skill_rank, id)` для `skill` (NULL skill_rank — пустая строка в `cursor.v`), `(name, id)` для `name_asc`, `(created_at, id)` для `recent`. Reliability батч считается только для текущей страницы. Убран `count: exact`
- ✅ `/search/teams` и `/search/players` — `useInfiniteQuery` с ключами `["teams-search", queryString]` и `["players-search", queryString]`. Метка «Результаты · N+»

### 1.5.3.8 Виртуализация списков — отложено

С infinite scroll и `limit=20` единовременно в DOM ~50–100 строк. Виртуализация (react-window) пока не нужна. Возвращаемся к вопросу, если профайлер покажет проблемы рендера на конкретном экране (>200 нод одновременно).

### Не входит

- Денормализованный `events.yes_count` (нужен для server-side `has_spots` в публичном поиске) — отдельная задача
- `usePaginatedList` ещё используется в `/search/venues`. Не удаляем, площадки в скоуп 1.5.3 не входили
- EXPLAIN ANALYZE baseline в `docs/tech/perf-baseline.md` — после 1.5.2 (seed)
- RPC `get_team_events_with_stats` — пока агрегат нужен только в одном роуте, переносим, если повторится в 2-3 местах
- Server-side rendering (SSR) для search-страниц — Telegram Mini App работает в WebView, smooth client-side hydration важнее SSR
- Кэш на Vercel Edge для публичных endpoint'ов — оставляем на 2.0
- Денормализация (members_count, last_event_at и т.п.) — добавляем точечно по запросу, не превентивно

