# Роадмап MVP 1.4 — Переработка дизайна

> **Контекст.** После MVP 1.3 (единая дизайн-система) приступаем к постраничной переработке интерфейса. Изменения ведём итеративно: для каждого экрана собираем обратную связь (текст, референсы, скриншоты), затем реализуем. Итерации 26 и 29 из MVP 1.3, не закрытые полностью, переносятся сюда как основа для соответствующих страниц.
>
> **Порядок работы.** Каждая итерация — один экран или логически связанная группа. Перед реализацией: прочитать `docs/design/design-system.md`, изучить референс/фидбэк от пользователя, описать план. После реализации: обновить документацию.

---

### Итерация 30: События — лента и детальный экран

**Цель:** завершить переработку экрана событий (итерации 26.1–26.3 из 1.3).

- ⬜ **30.1** Лента событий (`team/[id]/events/page.tsx`): заменить «Создать событие» на `IconButton +` в `ScreenHeader`, карточки с фото/градиентом, эйбрау-группы «Предстоящие»/«Прошедшие»
- ⬜ **30.2** Детальный экран события: `PhotoBanner` + триплет-метрика, карточка голосования, `AvatarStack` придут/не придут, секция «Управление» для организатора, `BottomActionBar`
- ⬜ **30.3** Список участников completed события: единый список с отметками «был/сдал»
- ⬜ **30.4** Обновить документацию: `[event]-detail.md`, `[event]-attendance.md`, `[event]-voting.md`

**Зависимости:** итерация 24 (выполнена в 1.3)

---

### Итерация 31: Полировка и аудит

**Цель:** завершить полировку (итерация 29 из 1.3) + аудит всех экранов после переработки.

- ⬜ **31.1** Поиск: убрать `bg-background-dark`, `font-display` вне разрешённых, хардкод-цвета
- ⬜ **31.2** Удалить из `globals.css` неиспользуемые токены
- ⬜ **31.3** Заменить unicode-эмодзи на SVG-иконки. Расширить `Icons.tsx`: `EditIcon`, `CheckIcon`, `XIcon`, `ChevronRightIcon`, `PinIcon`, `WalletIcon`, `MoreIcon`
- ⬜ **31.4** Заменить «Сохраняю…», «Создаю…» на `Button loading`
- ⬜ **31.5** Тест на 320px: чипы скроллятся, грид переносится, bottom-bar не перекрывает контент
- ⬜ **31.6** Пройти `ui-consistency-audit.md` — каждый пункт закрыт или явно в post-MVP

**Зависимости:** итерации 30

---

> Следующие итерации добавляются по мере получения фидбэка и референсов от пользователя.
